# プロジェクト概要

このプロジェクトのアプリ開発は、`planner` / `architect` / `generator` / `evaluator` の4サブエージェント(`.claude/agents/`)を `spec-pipeline` スキル(`.claude/skills/spec-pipeline/SKILL.md`)がオーケストレーションする自律開発フローで進める。決定論的なガードレールは `.claude/hooks/` の hooks が、案件をまたぐ教訓は `reference/learnings.md` が担う。

**オーケストレーター(呼び出し元Claude)がユーザーに向けて書く内容(チャット上の説明・進捗報告・確認質問など)は、常に日本語で記載する。** コードそのもの・コード識別子・技術用語の原語表記はこの限りではない。

## 起動方法

- 新規アイデアから始める場合: `/spec-pipeline <1〜4行のアイデア>`
- 既存specの途中スプリントから再開する場合: `/spec-pipeline <docs/spec/配下のspec.mdへのパス> [開始スプリント番号]`、または `/resume [開始スプリント番号]`
- 進捗確認: `/status` ／ 教訓の振り返り記録: `/retro`

## 4サブエージェントの役割分担(越境禁止)

| エージェント | モデル | 役割 | 出力 |
|---|---|---|---|
| `planner` | opus | アイデア→製品仕様書。「何を作るか」のみを定義し、技術スタック等の実装詳細には踏み込まない | `docs/spec/<slug>-spec.md` |
| `architect` | opus | 「どの土台で作るか」= 技術ベースライン(言語/FW/データ層/ホスティング/テスト基盤)を**1回だけ**確定。実行環境の制約と運用コストを必ず比較材料にする。コードは書かない | `docs/spec/<slug>-architecture.md` |
| `generator` | sonnet | 指定された**1スプリント分のみ**を、architectのベースラインに従って実装。合否は自分で判定せず、事実ベースの自己評価を残す | `docs/sprints/sprint-<N>-selfeval.md` |
| `evaluator` | opus | Playwright MCPでアプリを実際に操作して検証。テストGreenも合否に含む。**自分でコードは直さない**。基準を1つでも下回れば全体FAIL | `docs/sprints/sprint-<N>-evaluation.md` |

各エージェントは上記の役割以外に手を出さない設計。スプリント進行管理・PASS/FAILによるフロー分岐・gitコミット・`/simplify`等スキルの呼び出しはオーケストレーター(呼び出し元Claude)側の責務。

## 決定論ガードレール(hooks)

`.claude/settings.json` で以下の hooks を有効化し、モデルの遵守に頼らず危険操作を機械的に止める。フレームワーク第一原則「補助処理は本体を絶対に止めない」に従い、hookは高速・非ブロッキングを基本とし、止めるのは本当に危険な操作だけに限定する(hook内部エラーは fail-open)。

- **guard-secrets** (PreToolUse/Bash): `git commit`前に`git diff --cached`を走査し、`.env`・鍵ファイル・APIキー様の文字列が混入していたらコミットを拒否。
- **no-push** (PreToolUse/Bash): `git push`を一律拒否(このパイプラインは自動pushしない)。
- **protect-immutable** (PreToolUse/Edit·Write): `docs/dashboard.html`(凍結ファイル)の書き換えを拒否。
- **post-edit-check** (PostToolUse/Edit·Write): 変更ファイルをprettierで軽量整形(導入済みのときのみ・非ブロッキング)。

hookが操作を止めた場合、その理由が差し戻される。これはコード不備ではなく危険操作の検知なので、スプリントのリトライ上限とは無関係。

## セキュリティ・品質ゲート

4エージェントの合否基準(致命的バグ0件/コンソールエラー0件/受け入れ基準充足率100%/テストGreen)は**機能面のみ**で、セキュリティやコード品質は含まれない。そのため`evaluator`がPASSした直後に、オーケストレーターが追加ゲートを実行してから初めてスプリントを確定させる。加えて、evaluatorの検証**前**に`/simplify`で実装を自浄する(品質のみ・バグ探索はしない。simplifyの変更もevaluatorが検証するためPASS=検証済みの不変条件を壊さない)。

1. `git add -A` でそのスプリントの変更をステージ(まだコミットしない)。
2. `git diff --cached --stat` で、`.gitignore`対象(`.env`・鍵ファイル・認証情報等)が誤ってステージされていないか確認し、あれば `git restore --staged` で除外する(guard-secrets hookもコミット時に二重で守る)。
3. `/security-review`スキルをステージ済みの変更(`git diff --cached`)に対して実行する。
4. `/code-review`スキル(effort: `low`。Claude Code標準で追加コスト・外部依存なし)を同じ差分に対して実行し、evaluatorの機能テストにも`/security-review`のセキュリティ観点にも含まれない「正しさのバグ」「簡潔化・効率化の余地」を検出する。
5. 依存関係マニフェスト(`package.json`/`requirements.txt`等)を変更・追加した場合は、`npm audit`/`pip-audit`等の脆弱性スキャンも実行する。

- 上記いずれかでCritical/High相当の指摘、または`/code-review`が`CONFIRMED`と判定した正しさのバグ → そのスプリントをFAIL扱いにし、`git reset` でステージを解除した上で指摘内容を`generator`に渡して再実装(evaluatorのFAILと同じリトライ上限を共有)。
- 指摘なし、またはMedium/Low相当・`/code-review`の`PLAUSIBLE`判定や簡潔化提案等の軽微な指摘のみ → ステージ済みの変更を`git commit`してPASS確定、次のスプリントへ。

このレビューはgit差分ベースのため、プロジェクトはgitリポジトリで運用し、**スプリントが最終PASSした時点でオーケストレーターがコミットする**(スプリント境界=コミット境界)。generator/evaluatorはBashツールを持つがコミットは行わない。

## 横断学習メモリ(reference/learnings.md)

案件をまたいで再利用できる教訓を蓄積する常設ファイル。`planner`(プロダクト設計)・`architect`(技術選定)・`generator`(実装)はそれぞれ着手前に該当セクションを読み、既知の轍を避ける。オーケストレーターは、スプリントを「要人手介入」で確定させたとき(および`/retro`実行時)に、その根本原因を**一般化した教訓**として追記する。固有名詞・秘密情報は書かず、同じ教訓は更新・誤った教訓は削除して肥大化を防ぐ。

## コード側の品質・安全性の最低ライン(generatorの実装原則)

- シークレット(APIキー・トークン・認証情報)はソースコードにハードコードせず、`.env`(gitignore済み)経由で読み込み、`.env.example`にキー名のみ残す。
- 外部API呼び出し・ファイルI/O・ユーザー入力など信頼境界のエラーハンドリングを実装する(起こり得ない事態への過剰な防御は書かない)。
- 依存関係は保守されている実績あるものを最小限だけ追加する。
- **テストは必須(TDD-lite)**: architectが選定したテスト基盤で、明確な入出力・分岐・状態遷移を持つロジックには実装と同時に最小限のテストを書く。`evaluator`はテストスイートを実行し、1件でも失敗すればFAIL(テストGreenを合否基準に含む)。純粋な見た目のみの部分まで無理にテストは書かない。
- 起動方法・環境変数が変わったら`README.md`を最小限更新する。
- `architect`は技術ベースラインを、実行環境の制約(Mac依存/管理者権限の要否)と運用コスト(無料枠/月額/従量)を必ず比較材料にして選定する。`planner`は外部API利用規約・レート制限・個人情報や認証情報の取り扱いなど無視できないリスクを仕様書の「リスク・留意事項」に明記する。

## 運用ルール

- **リトライ上限**: 同一スプリントでFAILが続く場合、既定3回で「要人手介入」としてマークし、無限ループせず次のスプリントへ進む。理由なく上限を変更しない。
- **一時的な基盤障害とスプリントの合否は別カテゴリ**: 「temporarily unavailable」等、ツール呼び出し基盤側の一時的な瞬断による失敗はコードの不具合ではないため、リトライ上限を消費させず、FAILや要人手介入の根拠にもしない。同じ呼び出しを数回再試行すれば大抵解消する。詳細は`SKILL.md`参照。
- **スプリントは順序依存**: planner→architect→(スプリントごとに)generator→simplify→evaluatorの順で呼び出し、並列実行しない。異なるスプリントを同時に進めない。
- **コンテキストの分断**: 各サブエージェント呼び出しは独立コンテキストで動く。呼び出すたびに`docs/sprints/sprint-<N>-brief.md`のパス(主な参照先)・spec.mdのパス・architecture.mdのパス・対象スプリント番号・直前のFAILフィードバックを明示的にプロンプトへ含める。
- **ブレインストーミング**: 新規アイデアが曖昧な場合、`planner`呼び出し前にオーケストレーターがAskUserQuestionで2〜3問だけ確認する。詳細なアイデアには質問攻めにしない。
- **多重起動の検知**: パイプライン開始前に`docs/pipeline-status.js`の`phase`/`lastUpdated`を確認し、別セッションが実行中の可能性があればユーザーに確認を取ってから開始する。詳細は`SKILL.md`参照。
- **Playwright MCP必須**: `evaluator`の実機検証には`.mcp.json`で定義したPlaywright MCPサーバーが必要。ブラウザ本体未インストールが原因の失敗は`npx playwright install chromium`(無料・公式、管理者権限不要)を自動で試みてから再検証する。それでも失敗する場合のみ`Bash`でのAPI直叩き・静的確認に縮退し、evaluatorレポートの「検証モード」欄に明記した上でユーザーに報告する。
- **環境不足の自動補完**: ビルド・起動・テストが無料かつプロジェクトローカルなツール・パッケージの不足だけを理由に失敗する場合、`generator`/`evaluator`は「未検証」扱いにする前に自動でインストールしてから再試行する。管理者権限やOS全体への変更を要するインストールは対象外とし、その場合のみ手動インストール手順をレポートに残す。
- **キャッシュ衛生管理**: 上記の自動インストールを行った際は、ついでにnpm/pipキャッシュのうち直近60日以上更新されていないファイルを削除する。失敗しても実装・検証作業を優先する。Playwrightのブラウザキャッシュは対象外。
- **事前確認(外部サービス依存・管理者権限インストール予想)**: 仕様書のリスクに外部API/OAuth等ユーザー操作が必要な項目があれば、または対象プラットフォームが`デスクトップ`・機能に動画/音声処理・重量級ローカルDB・OS常駐等があり管理者権限インストールが予想される場合、スプリントループ開始前にAskUserQuestionで確認する。詳細は`SKILL.md`参照。
- **サーバーライフサイクル**: `generator`は自己確認用に起動したサーバーを自己評価レポート作成前に必ず停止する。`evaluator`は自分で起動し直し、ポートが使用中なら停止してから起動し直す。

## ディレクトリ規約

- `docs/spec/` — 製品仕様書(planner出力)・技術ベースライン(architect出力: `<slug>-architecture.md`)
- `docs/spec/<slug>-wireframes.html` — 画面を持つプロダクトの場合にplannerが作成する主要画面のグレースケール・ワイヤーフレーム。任意ファイル。
- `docs/sprints/` — スプリントごとの自己評価レポート・評価レポート(generator/evaluator出力)
- `docs/sprints/sprint-<N>-preview-*.png` — 画面を持つプロダクトでスプリントがPASSした際にevaluatorが保存するプレビュー画像。任意ファイル。
- `docs/dashboard.html` — 実行状況ダッシュボード(静的、書き換えない。protect-immutable hookで保護)。ブラウザで直接開くと30秒間隔で自動更新される。
- `docs/pipeline-status.js` — ダッシュボードのデータソース。オーケストレーターが状況変化のたびに上書きする実行時状態で、成果物ではないため`.gitignore`済み。
- `docs/sprints/sprint-<N>-brief.md` — 仕様書から対象スプリント分を原文のまま抜粋したファイル。generator/evaluatorはこれを主な参照先とする。
- `docs/project-memory.md` — プロジェクト全体の進捗・技術選定・既知の課題を集約した常設ダイジェスト。
- `docs/Home.md` — Obsidianで開いた際の入口(MOC)。上記ファイル群へのwikilinkを持つ。
- `reference/learnings.md` — 案件をまたぐ横断学習メモリ。
- `.claude/hooks/` — 決定論ガードレール(Node.js製のhookスクリプト)。
- `.claude/commands/` — カスタムスラッシュコマンド(`/status`・`/resume`・`/retro`)。

## トークン節約の原則

いずれも**要約や省略による情報欠落ではなく**、原文の再配置・肥大化防止・冗長さの削減によるもので、成果物のクオリティを落とさない(むしろ曖昧さに起因する誤判定・手戻りを減らす分、品質にも寄与する)。

- **原文抜粋の再利用**: `sprint-<N>-brief.md`を主な参照先にし、仕様書全体の読み直しを避ける。
- **不合格理由は要点だけを転記**: リトライ時、オーケストレーターはevaluatorレポートの「発見したバグ・問題点」や`/security-review`のCritical/High指摘そのものだけを次のgenerator呼び出しに転記する。
- **`architecture.md`は追記でなく上書き**: 同じ領域の技術選定を再検討したら古い記載を置き換える。決定の変遷ではなく「現在の決定」だけを保持する。`learnings.md`も同様に更新・削除で肥大化を防ぐ。
- **レポートは簡潔な箇条書き**: generator/evaluatorの各種レポートは長いプローズを避ける。
- **受け入れ基準は具体的・検証可能に**: 曖昧な基準は誤判定や無駄な再実装サイクルに直結するため、plannerの時点で手順として読める粒度まで具体化する。
